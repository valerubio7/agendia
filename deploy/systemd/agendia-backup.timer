[Unit]
Description=Schedule Agendia production backup

[Timer]
OnCalendar=*-*-* 03:30:00
RandomizedDelaySec=30m
Persistent=true
Unit=agendia-backup.service

# Deliberately no [Install] section: this timer remains disabled until an external backup provider and restore evidence are accepted.
