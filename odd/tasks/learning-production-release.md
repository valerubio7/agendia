# Learning production release

Goal: publish an immutable GHCR image and deploy Agendia to the learning production host. The user accepts the learning-project vulnerability exceptions and production without a tested backup/restore. Never claim deployment without a real digest, working external config/secrets, host execution and smoke evidence. Never put secrets in Git or chat; give one short, verifiable host step per turn.

Decision: preserve the Trivy scan and malformed-report rejection. UNKNOWN and CRITICAL findings are accepted for this learning release; the 20 HIGH IDs retain exact, expiring exceptions. The formal staging → production route uses `scripts/deployctl.ts` from a pinned root-owned checkout, not the intentionally disabled host-bundle stub. No SDD selection. ODD TDD mode: off by the user's earlier explicit choice; focused runner for P3a: Bun 1.4.0 `bun test tests/contracts/host-runner.contract.test.ts`. Review mode was off at the last check; recheck before a candidate. Delivery strategy: ask-on-risk; forecast P3a ~100–180 authored changed lines, below the 400-line review heuristic.

Worktree for P3a: `fix/host-preflight-workspaces` at `/home/valerubio7/Projects/agendia-host-preflight`, based on `d84d91d16e0622d248250e50414e13f43598a3f0`. The original checkout and the preexisting uncommitted notes in `/home/valerubio7/Projects/agendia-release-unblock` remain untouched.

## Tasks
- [x] P1 — UNKNOWN release policy and focused contract; preserve Trivy, CRITICAL and malformed-report gates. Delegated writer and independent verifier. Focused contracts, Biome and TypeScript passed. Commit `4054d7b7b65971722350fb15bf8870677d540782`.
- [x] P1b — Accept CRITICAL findings for the learning release while preserving the scan and HIGH exact/expiring policy. Delegated writer and independent verifier; contracts, Biome and TypeScript passed. Commit `f689e5c935d9e765383928439613990be3dbeb72`.
- [x] P1c — Parse Docker's real SHA-tag push digest summary and reject malformed/mixed summaries. Delegated writer and independent verifier; contract, Biome and TypeScript passed. Commit `41df8b06426762eafab2589b324a40064a6dddd1`.
- [x] P2 — Release GHCR immutable image: PR #34 squash merge `d84d91d16e0622d248250e50414e13f43598a3f0`, main CI 35877089334 6/6, Release 35877715551 PASS; digest `ghcr.io/valerubio7/agendia@sha256:e5d4213b404b9f5135fca6e195bd6aa24f38238aa3e76bb3a954df242f4c0c27`, artifact 10759166946 (11 files). CI verified immutable identity. Work-unit commit `41df8b06426762eafab2589b324a40064a6dddd1`.
- [ ] P3a — Correct host source preflight for Bun 1.4.0 workspaces without weakening ignored-source drift protection (approved issue #35). Route: delegated worker (source, contract, runbook; multi-file write); independent verifier required because native ASSESS was unavailable (`package-local-binary-missing`, tier unassessable). Host evidence: frozen lock install produced exactly seven `!! .../node_modules/` paths; old preflight accepted only root. Implemented an order-independent exact seven-path check (reject omissions, duplicates, extras) and a matching runbook guard using `find -L` so valid Bun symlinks do not look writable; preserve clean-tree and ownership/mode checks. Writer and independent verifier both observed Bun 1.4.0 focused contract 9 pass, Biome pass and diff-check pass; parent spot check repeated 9 pass and diff-check. No work-unit commit or delivery yet. Do not patch or delete dependencies on the server to evade the old guard.
- [ ] P3 — Deploy via formal staging → production with real external configs and secrets, approved plan and separate staging/production authorization; verify status/smoke. Server: SSH/Herdr/Docker and pinned Bun 1.4.0 work, detached checkout d84d91d root-owned, frozen deps installed and 56 dependency files had unsafe write bits removed; recursive owner/mode checks now pass. `/etc/agendia/staging/config` and `/etc/agendia/staging/secrets` are absent; production config also absent. Critical: `scripts/deployctl.ts` requires command commit to match `approvedPlan.snapshot.audit.commit` and the executing checkout, so this fix cannot run with the old published image's d84d91d evidence; formal delivery requires a new CI/Release image digest and matching plan, not a host patch. Need refreshed checkout after P3a delivery, staging/production external inputs and approvals. No application deployment yet. Host steps must remain short, one verifiable action at a time. Evidence pending; do not claim deployment.
